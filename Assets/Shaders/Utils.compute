#ifndef __UTILS_INCLUDED__
#define __UTILS_INCLUDED__ 1


bool Equals(float3 a, float3 b)
{
    return a.x == b.x && a.y == b.y && a.z == b.z;
}

bool Equals(uint3 a, uint3 b)
{
    return a.x == b.x && a.y == b.y && a.z == b.z;
}

bool Between(uint x, uint min, uint max, bool includeBounds = true)
{
    return includeBounds ? min <= x && x <= max : min < x && x < max;

}

#endif